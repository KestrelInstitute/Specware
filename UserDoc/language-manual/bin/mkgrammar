#!/bin/sh
# this is bin/mkgrammar

(/bin/cat metaslang.glm; /bin/echo ZeEnd) | bin/glmgrammar > grammar.glm.body
cat grammar.preamble >> grammar.glm
cat grammar.glm.body >> grammar.glm
cat grammar.postamble >> grammar.glm
