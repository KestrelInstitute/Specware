(* This spec collects all the specs that form the Specware general library.
Thus, a Specware development that uses the general library just needs to import
this spec. If not all the general library is needed or wanted, individual specs
in the subdirectory General can be of course used. *)

spec

  import General/Order
  import General/FiniteMap
  import General/Assert

endspec
