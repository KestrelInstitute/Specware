There is a progr

  let
    def handler except = { }
    def prog = { }
  in
    catch prog handler
 
This causes problems unless we add an argument () to prog.
Perhaps the parsing is wrong as prog way be required to have 
an argument
